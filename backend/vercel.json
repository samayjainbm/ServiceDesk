{
  "version": 2,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index",
      "buildCommand": "npx prisma generate && npx prisma db push",
      "installCommand": "npm install"
    }
  ]
}